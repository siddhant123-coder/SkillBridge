export const APP_NAME = 'SkillBridge';

/** Minimum digits required for a valid Indian mobile number (excluding country code). */
export const PHONE_NUMBER_LENGTH = 10;

export const DEFAULT_COUNTRY_CODE = '+91';
